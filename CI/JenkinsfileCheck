pipeline {
    agent {
        label 'openshift-prg'
    }

    stages {
        stage('SCM Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Check') {
            steps {
                withCredentials([sshUserPrivateKey(credentialsId: 'sftp-countries', keyFileVariable: 'keyFile', usernameVariable: 'user')]) {
                    sh """./CI/schema-validation.sh ${keyFile}"""
                    sh """rm keyfile"""
                }
            }
        }
    }
    post {
        always {
            archiveArtifacts artifacts: 'schema_validation/logs/import-validation.log', fingerprint: true
        }
    }


}
